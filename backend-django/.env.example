DEBUG=True
SECRET_KEY=your-secret-key
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# NOTE: When running via Docker Compose, this is automatically injected from the root .env.
# Uncomment and adjust below ONLY if running the backend locally without Docker:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/scrumban_django